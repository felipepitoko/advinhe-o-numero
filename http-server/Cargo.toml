[package]
name = "http-server"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = "4"
chrono = "0.4" # For timestamps
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["v4"] }
serde_json = "1"